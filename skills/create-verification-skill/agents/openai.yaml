interface:
  display_name: "create-verification-skill"
  short_description: "Use pstack's create verification skill workflow."
  default_prompt: "Use $create-verification-skill for this task."
policy:
  allow_implicit_invocation: false
