interface:
  display_name: "principle-experience-first"
  short_description: "Use pstack's principle experience first workflow."
  default_prompt: "Use $principle-experience-first for this task."
policy:
  allow_implicit_invocation: false
