# Fable approval record

On 2026-09-18, two completed **Claude Fable 5.1** reviews at requested **xhigh** returned **approve** for code commit [`64716ccce5b5`](https://github.com/J0UH/pstack-codex/commit/64716ccce5b58e7195d20789aafaf7d9f88c7c61). One assessed workflow fidelity and the other assessed the standalone CLI runtime.

This is approval for the documented **limited alpha**, not a claim of complete Cursor runtime parity. The reviewers received the pinned original pstack instructions, both earlier findings reports, the fixed candidate source and diff, and parent-observed test/live evidence. They performed source review; they did not execute their own tests. The initial workflow review required changes before this approval.

The CLI's substantive assistant messages identified `claude-fable-5-1`; both calls completed with structured verdicts and no MCP access. Only the schema-output tool was enabled. Effort was requested explicitly; internal reasoning compute was not independently measured. Raw prompts, transcripts and account metadata remain private.

The reviewed candidate passed 87 Python tests and 52 unchanged upstream tests. Focused live checks covered scoped reader Git, permitted/denied writer paths, safe-mode suppression with a positive control, and mode activation/identity/selection across a worktree and resumed conversation. See [verification](verification.md) and the [complete sanitized verdicts](../evidence/fable-review.json).

## Workflow verdict

Workflow scope of commit 64716ccce5b58e7195d20789aafaf7d9f88c7c61 as a limited alpha: (1) the byte-preserved upstream library (47 registered skills, 23 playbooks, 23 principles, 2 agent roles, 3 path-loaded companions, 3 dormant Benny skills) with only the two ledgered transformations; (2) explicit-only discovery matching upstream (46 false, setup-pstack true; companions carry no policy files); (3) conversation-local mode lifecycle whose activation fires from the published plugin and skill default prompts, whose hook context supplies the authoritative session/project identity and command prefix, and whose CLI resolves the recorded context instead of the shell cwd; (4) the Codex model-role setup procedure, JSON schema and validator preserving upstream role labels, panel-count and inheritance semantics with no model substitution; (5) limitation disclosure for durable wakeups, cloud placement, plan checker, transcripts, Benny, webhooks and Grok, with playbook stopping rules unchanged. NOT approved: unattended continuation of Autonomous run, Babysit drive, Shipping watch, either Autopilot, Orchestrate or unattended Hillclimb; Cursor runtime parity; Grok inference; end-to-end fidelity of the 21 playbooks not exercised live; runtime adapter correctness (other reviewer's scope); and any reading of this review as independent reproduction of the parent's live proofs. Approval assumes `python3 scripts/build.py --check` and `python3 scripts/package.py --check` pass at this exact commit, which the filtered diff did not let me confirm (adaptations.json, companion openai.yaml deletions and plugins/ copy were not shown). The four P3 findings may ship as recorded follow-ups.

## Runtime verdict

Runtime layer of commit 64716ccce5b58e7195d20789aafaf7d9f88c7c61 as a limited alpha: the Claude analysis/reader/writer adapters over the installed-CLI login route with scoped Bash rules for readers and a single cwd-anchored Edit rule for writers; the common launcher's timeout, normal-exit and handled-signal (SIGINT/SIGTERM/SIGHUP) lifecycle as tested with real subprocesses; fail-closed model/tool/receipt evidence including retained-but-unaccepted partial output; and Grok gated as unsupported (reader/writer) or blocked-before-inference (analysis) with the uniform receipt schema. What I verified from source: adapter validation, argv construction, stream parsing, evaluate precedence, signal deferral and termination logic, and that the new tests assert the claimed behaviors. What I accept only as parent-observed sanitized evidence consistent with the code, not independently reproduced: that Edit(/**) governs Write and anchors at cwd on the installed Claude Code, that safe mode suppressed the sentinel hook/MCP/instruction in the probe, that the reader ran git log live, and that the exact Grok argv produced a sandbox error rather than an unknown-option error. Approval does NOT cover: Grok inference or any Grok profile as live; gh-authenticated or complete seven-source why investigations; effort as applied reasoning compute; managed-policy customizations under safe mode; Edit(/**) anchoring when cwd is a subdirectory of a larger repository; SIGKILL/hard-kill reconciliation beyond the documented procedure; Bash rule wildcard semantics beyond the forms live-tested (bare-wildcard and :* prefix); or any workflow-scope claim. The 87 Python and 52 Bun test passes are parent-reported; I counted 48 runtime tests by reading and did not run them.

## Limits

Grok remains optional and unverified for live inference. Durable wakes, cloud placement, Grok Bot/Benny integrations and some transcript integrations are unavailable without separately verified host adapters. The unchanged plan checker retains its original host/model assumptions. Workspace-write mode needs authorized write access to the shared mode-state directory. No side-by-side Cursor execution baseline was run.

Approval binds the code commit above. A subsequent publication commit adds these review records and updates documentation/package metadata; it does not extend the approval to future code changes.

## Nonblocking workflow follow-ups

The workflow reviewer allowed four P3 follow-ups to ship with the alpha. Publication documentation now specifies the verified first-line activation form, includes the scoped reader probe in status, and names unattended Hillclimb and Visual parity loops among the unsupported wake-dependent paths. The schema and Python validator agree on the inspected rules, but broader schema/validator differential testing remains a maintenance follow-up. The quoted `<action>` in injected command templates must be replaced with a real action; it is deliberately not an executable action name. Changing it to an unquoted shell metacharacter placeholder is not required for this approval. Runtime follow-ups, if any, are preserved verbatim in the JSON record.

The workflow approval also required build and package checks at its exact target. Both `python3 scripts/build.py --check` and `python3 scripts/package.py --check` passed on that clean commit; companion policy deletions and generated copies are included in it.
