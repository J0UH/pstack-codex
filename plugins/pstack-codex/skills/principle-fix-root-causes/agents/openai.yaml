interface:
  display_name: "principle-fix-root-causes"
  short_description: "Use pstack's principle fix root causes workflow."
  default_prompt: "Use $principle-fix-root-causes for this task."
policy:
  allow_implicit_invocation: false
