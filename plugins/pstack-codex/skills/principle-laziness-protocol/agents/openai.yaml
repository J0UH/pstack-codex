interface:
  display_name: "principle-laziness-protocol"
  short_description: "Use pstack's principle laziness protocol workflow."
  default_prompt: "Use $principle-laziness-protocol for this task."
policy:
  allow_implicit_invocation: false
